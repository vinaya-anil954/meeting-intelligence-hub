# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/meeting_hub

# OpenAI API key — required for AI extraction and chat features
# Without this, the app uses keyword/regex fallback extraction
OPENAI_API_KEY=your-openai-api-key-here

# Port for the Express server (default: 5000)
PORT=5000
